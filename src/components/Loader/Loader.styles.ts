import { makeStyles } from '@material-ui/core'
import { VERDEAGUA } from '../../shared/styles/colors'

export const loaderUseStyles = makeStyles(() => ({
  colorPrimary: {
    color: VERDEAGUA
  },
  container: {
    justifyContent: 'center'
  }
}))
